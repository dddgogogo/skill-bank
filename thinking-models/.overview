Decision-analysis, problem-solving, and critical-thinking mental models (from cc-thinking-skills). Select the ONE whose method best fits coaching the action under review.

- `builtin/thinking-models/archetypes`: thinking-archetypes — The same problem keeps recurring despite fixes, growth stalled with no obvious cause, or a quick fix made things…
- `builtin/thinking-models/bayesian`: thinking-bayesian — Interpreting a test result, metric, or new evidence and you risk over-reacting to it.
- `builtin/thinking-models/bounded-rationality`: thinking-bounded-rationality — A search or investigation could run indefinitely and you need a stopping rule.
- `builtin/thinking-models/circle-of-competence`: thinking-circle-of-competence — You're unsure whether you actually know the answer.
- `builtin/thinking-models/cynefin`: thinking-cynefin — Unsure how to approach a problem—plan, analyze, experiment, or stabilize first.
- `builtin/thinking-models/debiasing`: thinking-debiasing — In a long trajectory where you're defending a path you committed to early, run a quick self-check for sunk-cost and…
- `builtin/thinking-models/dual-process`: thinking-dual-process — An answer arrives too fast on a high-stakes or unfamiliar task.
- `builtin/thinking-models/effectuation`: thinking-effectuation — Start with means, not goals; co-create with partners; leverage contingencies.
- `builtin/thinking-models/feedback-loops`: thinking-feedback-loops — A system shows runaway growth/collapse, oscillates around a target, or resists change, and you need to find the loop…
- `builtin/thinking-models/fermi-estimation`: thinking-fermi-estimation — You need a number you can't measure and can't look up.
- `builtin/thinking-models/first-principles`: thinking-first-principles — When a constraint is treated as fixed ("too expensive", "impossible", "always done this way"), ask whether it's physics…
- `builtin/thinking-models/five-whys-plus`: thinking-five-whys-plus — A fault is localized to a component and the proximate cause is known but the systemic root is not — chain 'why' with…
- `builtin/thinking-models/inversion`: thinking-inversion — When planning work where optimism may be hiding risks, ask "how would I guarantee this fails?" — enumerate failure…
- `builtin/thinking-models/jobs-to-be-done`: thinking-jobs-to-be-done — Deciding what to build or why a feature isn't adopted.
- `builtin/thinking-models/kepner-tregoe`: thinking-kepner-tregoe — A defect is selective (some endpoints/regions/users/times affected, not all) and the cause is unclear — map what IS vs…
- `builtin/thinking-models/leverage-points`: thinking-leverage-points — Picking where to intervene in a system and tuning parameters keeps not sticking—rank candidate interventions by…
- `builtin/thinking-models/lindy-effect`: thinking-lindy-effect — Choosing a technology/framework/dependency and longevity matters.
- `builtin/thinking-models/map-territory`: thinking-map-territory — Behavior contradicts the docs, tests, diagram, or your assumption — stop reasoning from the description, go verify the…
- `builtin/thinking-models/margin-of-safety`: thinking-margin-of-safety — Provisioning capacity, setting a timeout/limit, or committing to an estimate under uncertainty.
- `builtin/thinking-models/model-combination`: thinking-model-combination — Combine multiple mental models for richer analysis.
- `builtin/thinking-models/model-router`: thinking-model-router — Route to the right mental model based on your domain and problem type.
- `builtin/thinking-models/model-selection`: thinking-model-selection — Choose the right mental model for the problem at hand.
- `builtin/thinking-models/occams-razor`: thinking-occams-razor — Multiple causes could explain a bug — test the fewest-assumption hypothesis first; escalate to complex only when…
- `builtin/thinking-models/ooda`: thinking-ooda — Use under time pressure (incident, outage, debugging a moving target) when you must act before you have certainty—cycle…
- `builtin/thinking-models/opportunity-cost`: thinking-opportunity-cost — Before committing scarce time/people/money to one thing, ask "what's the next-best use of these resources, and what…
- `builtin/thinking-models/pre-mortem`: thinking-pre-mortem — Before committing to a plan or launch, assume it has already failed and reason backward through why — prospective…
- `builtin/thinking-models/probabilistic`: thinking-probabilistic — Stating a forecast, estimate, or risk.
- `builtin/thinking-models/red-team`: thinking-red-team — Reviewing code, auth, or APIs for security vulnerabilities — adopt an attacker mindset, enumerate the attack surface…
- `builtin/thinking-models/regret-minimization`: thinking-regret-minimization — When advising a human on a high-stakes, hard-to-undo life/career choice, weigh the asymmetry between a recoverable…
- `builtin/thinking-models/reversibility`: thinking-reversibility — Before deliberating over a decision, ask if it's a one-way door (costly to undo) or two-way (cheap to undo) — decide…
- `builtin/thinking-models/scientific-method`: thinking-scientific-method — A symptom could have several causes and you must find the faulty code by ranking falsifiable hypotheses and checking…
- `builtin/thinking-models/second-order`: thinking-second-order — When a change has effects past the immediate fix (incentives, scale, feedback loops), ask "and then what?" across…
- `builtin/thinking-models/socratic`: thinking-socratic — Use before building when a request is vague, assumption-laden, or "obvious." Ask the clarifying questions that surface…
- `builtin/thinking-models/steel-manning`: thinking-steel-manning — Use before rejecting a proposal or when you're inclined to just agree with the user.
- `builtin/thinking-models/systems`: thinking-systems — Debugging across services/an incident where a fix in one place breaks another, or behavior is emergent and no single…
- `builtin/thinking-models/theory-of-constraints`: thinking-theory-of-constraints — Optimizing latency or throughput in a pipeline and one stage dominates—focus all effort on that single bottleneck…
- `builtin/thinking-models/thought-experiment`: thinking-thought-experiment — You need to trace how a system would fail or behave at a scale you can't cheaply test or measure.
- `builtin/thinking-models/triz`: thinking-triz — Stuck between two architecture or API requirements that seem mutually exclusive — name the contradiction precisely…
- `builtin/thinking-models/via-negativa`: thinking-via-negativa — Add a feature/layer/process to fix a problem.
